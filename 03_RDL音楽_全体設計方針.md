# RDL音楽｜全体設計方針

*T3：応用層 / CREATE・MATH / DRAFT v0.3*
*依存：RDL_Core 現行T0 / RDL_Modules 現行T1 / T2汎用検査道具候補*
*上位：RDL音楽理論*

---

## ■ 0. 目的

RDL音楽理論は、音楽をいきなり文化的な意味や既存の名称から説明しない。

まず、比較的変更されにくく検証しやすい関係から出発し、そこから知覚・学習・文脈・意味へ段階的に上がる。

```text
硬い関係を確認する
        ↓
観測条件を分ける
        ↓
知覚条件を分ける
        ↓
学習された構造を分ける
        ↓
文脈・意味・目的を扱う
```

ここでいう「硬い」とは、絶対に正しいという意味ではない。

```text
硬い
≠
絶対的に正しい
```

現在のBとΓのもとで、関係が比較的安定して検出・再現されるという意味で用いる。

したがって、硬さは対象に永久に埋め込まれた属性ではなく、どのBとΓで、どの関係が、どこまで保存されたかという検査履歴を伴って扱う。

旧整理でT2候補と呼んでいたcandidate、selection、commitment、record、alternative memoryなどの汎用処理位相は、Music固有理論そのものとして増殖させない。現行T0/T1基準では、それらをT1工程・T2汎用検査道具・Music固有Selection基準・historical evidenceへ再分類する。Music本線では、実際の音楽対象に対して、Bを置き、関係を抽出し、有限作用断面を明示し、保存条件と変化条件を指定し、生成・変形・再聴取まで戻す小さい検証を優先する。

---

## ■ 1. 設計の基本単位

RDL音楽では、対象だけを単独で記述しない。

```text
対象側SILN_music / W_music
× B（有限境界・Purpose・比較条件）
× RIB_B（実際に取得された有限作用断面）
× Γ（関係抽出・比較・破断判定規則）
↓
現在扱える関係記述
```

したがって、同じ対象でもB、実際に取得されたRIB_B、またはΓが変われば、保存・変化・破断の記述が変わりうる。

### 1.1 B

Bは、その層で何を取り出し、どの範囲を比較するかだけでなく、どのPurposeのもとでどのinteraction/action sectionを有限に扱うかを定める。

少なくとも次を有限に固定する。

```text
Purpose
対象範囲
interaction / action section
選択する関係・次元
比較条件
提示・観測条件
```

物理側では、具体的な条件として少なくとも次を含みうる。

```text
観測時間窓
時間分解能
サンプリング間隔
観測格子の開始位相・配置
```

知覚側・音楽側では、次のような境界が候補になる。

```text
可聴帯域
時間的統合範囲
音高単位
拍・小節
フレーズ
曲全体
提示順
聴取・分析対象範囲
```

Bは固定的な容器ではなく、目的と層に応じて引き直される。

Bは意図的に設定される境界だけでなく、身体・装置・環境によって事実上課される境界も含む。

同じ曲・同じ波形でも、Purpose、聴取者、分析器、時間窓、提示順、選択関係、比較条件が変われば別の有限Bとして扱いうる。

有限Bを置く限り、なお未回収関係ξが残る。

### 1.2 Γ

Γは、Bの中で関係をどう取り出し、何を同じものとして比較し、どの差を破断として扱うかを定める。

例：

```text
Γ_point_ratio       点サンプル上の周波数比を比較する
Γ_periodicity       周期・反復を抽出・比較する
Γ_pitch             音高関係を抽出する
Γ_meter             拍・アクセントの関係を抽出・比較する
Γ_context           現在文脈との整合・破断を判定する
```

同じBでもΓが変われば、回収される対象側関係記述やM_B候補、未検査領域の位置づけは変わりうる。ただしΓはξを直接回収・操作する規則ではない。

Γは、現時点ではRDL音楽_Coreの状態変数でも、RDL_Coreへ追加される新しい基底量でもない。

```text
Γ ∉ RDL基底量
Γ ∉ RDL音楽_Coreの状態変数
```

Γは、SILN耐久検査の検査原理を各領域へ具体化した、Adapter・Module・検証系の局所規則として扱う。

概念的には、少なくとも次の機能を含みうる。

```text
Γ
=
extract
+ compare
+ preserve-test
+ break-test
```

ただし、すべてのΓが常に四機能を持つとは限らない。実験ごとに、どの機能を担うΓであるかを明示する。

Γは「証明書」そのものではない。Γを用いた検査の結果として、

```text
どのBで
どのRIB_Bを実際に取得し
どのΓを使い
何が保存され
どこで破断し
何が未検査・未回収として残ったか
```

という条件付きの耐久記録を得る。

したがって将来的には、

```text
B + RIB_B + Γ
  ↓ 検査
対象側関係記述 / M_B候補
  +
耐久検査記録
```

という形を、構造耐久証明書や耐久証明付きSILN候補へ接続できる可能性を保持する。

ただし現段階では、Γを通過したことをもってSILNの絶対的耐久が証明されたとはみなさない。

### 1.3 M_B

M_Bは、現在のBにおいて対象側へ埋め込まれた安定構造ではなく、Bのもとで現在の解釈・予測・選択・応答・更新を拘束する自己側の有限関係拘束構造である。

```text
対象側:
  SILN_music / W_music

有限作用断面:
  RIB_B

自己側:
  M_B^device
  M_B^analysis
  M_B^listener
  M_B^learned
  M_B^context
```

検証で扱う「M_B候補」は、対象側の関係構造そのものではなく、特定のBとΓのもとで、実際に取得されたRIB_Bを読む自己側の関係拘束構造を暫定的に置いた結果である。

現段階では、M_B候補について次の操作的な検証判定を採用する。

```text
M_B候補
=
現在の有限BとΓのもとで
実際に取得されたRIB_Bを通じて提示された対象側SILN/Wを
解釈・予測・選択・応答・更新するために
破断なく使えている自己側の有限関係拘束構造
```

これは、連続時間や別の観測条件において実際に破断がないことを保証しない。

Core v2.0で導入されたC_rel / I(M_B)は任意の記述補助として扱う。Musicで使う場合は、自己側M_B、拘束される可能域、B・問い・時間断面、測定規則と検証条件を指定する。音響descriptorやfixture重みから、人間聴取上の拘束強度を直接確定しない。

```text
観測上の保存
≠
対象の絶対的な保存
```

Bを引き直すことで、以前のBでは未回収だった関係の一部が新しいBのRIB_Bを通じて記述可能になることはありうる。ただし、これはξを直接操作・回収することではなく、新しい有限Bで再観測し、そのBでなお残る未回収関係を改めて記述することである。

```text
interaction
  ↓ finite B
RIB_B
  with ξ(B) ≠ 0

change B / relation configuration
  ↓
new interaction
  ↓ finite B'
RIB_B'
  with ξ(B') ≠ 0
```

---

## ■ 2. 階層モデル

RDL音楽の全体は、次の四層で設計する。

```text
物理側の観測層
        ↓
基層：身体・知覚による変換関係
        ↓
中核：後天的に学習された音楽構造
        ↓
上層：現在文脈・意味・目的
```

この階層は価値の上下ではなく、主に次の差を分けるためのものである。

```text
何が対象側に近いか
何が人間系による変換として現れるか
何が形成に時間を要するか
何が現在文脈で変わりやすいか
```

最短には次のように読む。

```text
物理
何が変化したか

基層
その変化が人間系に
どのような差として現れたか

中核
その差を既習の音楽構造が
何として扱ったか

上層
今この文脈で
それが何を意味するか
```

### 2.1 物理側の観測層

波形、周波数、位相、時間変化、成分間の比などを扱う。

この層のFは、対象そのものではなく、有限Bで切り出された物理側作用断面 `RIB_B^phys` を、検出機器・分析器の \(M_B^{device}\) が解釈した結果として扱う。

```text
physical interaction
  ↓ B_phys
RIB_B^phys
  ↓ interp(M_B^device)
F_device
  × Γ_phys
  ↓
対象側関係記述 / M_B^device候補 / 検証記録
```

したがって、波形検出上のFと、人間の身体・知覚・学習済み音楽構造を通ったFは同一視しない。

```text
F_device ≠ F_human
```

06〜09の波形実験は、主にこの層に属する。

ここで確認するのは音楽的な意味ではなく、次のような関係である。

```text
値が変わっても比が保存される
関係の破断が観測分解能で見落とされる
同じ分解能でも格子配置で検出結果が変わる
```

物理モデルに破断があること、観測時間窓と交差すること、サンプル点で検出されることは別々に記録する。

### 2.2 基層：身体・知覚による変換関係

基層は、人間の中にあらかじめ完成した一枚の固定層として置かない。

また、神経科学や聴覚科学の既知概念を先に並べ、それをそのまま基層とみなすこともしない。

基層で問うのは、

```text
人間は何を持っているか
```

よりも、

```text
どういう物理状態の変化に対して
人間系がどういう状態変化・応答を示すか
```

である。

したがって、基層を現段階では次のように仮設する。

> **基層とは、物理状態の変化が、人間系においてどのような知覚可能な差・まとまり・分離・時間的応答として現れるか、その変換関係をBとΓのもとで記述する仮設ネットワークである。**

この意味で、基層は単なる中間箱ではなく、物理側の関係ネットワークと、後天的に学習された音楽構造のネットワークが接続する結節領域として扱う。

```text
物理状態 P_t
   ↓ ΔP

人間系による変換
   ↓

知覚・行動上の状態差 ΔR
   ↓

M_B^learned による
音楽的分類・予測・解釈
```

基層の中心は、このうち

```text
ΔP
 ↓
人間系による変換
 ↓
ΔR
```

の部分に繰り返し現れる関係である。

#### 2.2.1 F_device と F_human の間

RDL_CoreではFはM_Bによる作用解釈であるため、`F_device → F_human`を単純な同一系列のFとして扱わない。

概念的には、装置側作用と人間側作用を別の有限B/RIB_Bとして分ける。

```text
physical interaction
  ↓ B_device
RIB_B^device
  ↓ interp(M_B^device)
F_device
  ↓ transduction / presentation
human-side interaction
  ↓ B_human
RIB_B^human
  ↓ interp(M_B^human)
F_human
```

したがって、`F_device` をそのまま人間側M_Bの入力と同一視しない。装置出力がどのように提示され、人間側でどの有限作用断面として取得されたか自体を基層検証の対象として残す。

```text
F_device ≠ RIB_B^human
F_device ≠ F_human
```

#### 2.2.2 基層をどう抽出するか

基層は、物理側と中核側をそれぞれ先に確定してから機械的に埋めるものではない。

物理側の観測と、知覚・行動・learned音楽記述の間に現れる、

```text
安定写像
写像不能
破断
残差
```

を比較し、その中で繰り返し残る変換関係をM_B^base候補として仮設する。

このとき、既知の聴覚・身体・知覚・認知系の概念は `50_既知基層解釈参照` に参照基準点として置く。ただし、既知概念を置いたことをもって基層M_Bが確定したとは扱わず、B_baseとΓ_baseを明示した独立検査へ送る。

特に、次の不一致は基層候補を見つける重要な入口になる。

```text
物理的には違う
→ 人間には同じ差・まとまりとして現れる

物理的には同じ
→ 人間には異なるものとして現れる

物理的変化は検出できる
→ 人間は差として扱わない

小さな物理差しかない
→ 人間側では大きな状態変化になる
```

概念的な抽出手順は次の通りとする。

```text
1. 物理側の状態差 ΔP と、その有限B/RIB_Bを記録する
2. 人間側の知覚・行動上の状態差 ΔR と、その有限B/RIB_Bを記録する
3. learned音楽構造による分類・意味づけを別に記録する
4. B_base と Γ_base を変えて対応関係を検査する
5. 複数条件でも残る変換関係を M_B^base候補とする
6. 条件変更で大きく変わる部分は learned / context 側との接続として残す
7. 回収できない差・未検査領域は ξ として保持する
```

#### 2.2.3 基層と中核を最初から固定分離しない

基層と中核の境界は、単純な「生得／後天」だけで切らない。

例えば、ある反応が文化・学習歴・文脈を大きく変えても比較的残るなら、基層側の耐久候補として扱える。

逆に、同じ物理入力でも学習歴や音楽文化によって応答が大きく変わるなら、中核の `M_B^learned` への依存が強い候補として扱う。

```text
文化を変える
学習歴を変える
年齢を変える
文脈を変える
刺激条件を変える
        ↓ Γ_test

なお残る関係
        ↓
基層側の候補

大きく変わる関係
        ↓
learned / context 側への依存候補
```

これは絶対的な分類ではなく、BとΓを変えながら耐久性を比較する操作的な区別である。

#### 2.2.4 基層知覚条件の内訳

候補には次がある。

```text
可聴帯域
時間分解能
急激な変化への反応
周期・反復への感度
音のまとまり・分離
```

ただし、これらをすべてM_Bと一括しない。

```text
基層知覚条件
=
B_base
+ RIB_B^base
+ Γ_base
+ 基層で比較的安定するM_B候補
+ ξ_base
```

身体的な入力制約、有限作用断面、関係抽出規則、知覚上のまとまり、未回収成分は必要に応じて分けて記述する。

### 2.3 中核：後天的に学習された音楽構造

経験・反復・教育・参加した音楽環境・文化によって蓄積された関係構造を扱う。

候補には次がある。

```text
音高関係
音程
拍・アクセント
和声
旋律・モチーフ
様式的な予測
```

ここでのM_Bは、誰にとっても普遍的な音楽法則とは限らない。

```text
M_B^learned
=
経験によって学習され、
現在の音楽を比較するために再利用される関係構造
```

ここでの`learned`は形成過程を示す名称であり、RDL音楽_Coreの`Core`とは別の概念である。

12平均律、長短調、機能和声、ジャズ理論などは、この層に接続される具体的なM_Bモジュール候補であり、Coreそのものではない。

基層との違いは、「音楽ラベルを持つかどうか」だけでは決めない。

```text
基層
= 物理状態差が人間系でどのような差として現れるか

中核
= その差を既習音楽構造が何として扱うか
```

この区別も、実際の検証によって引き直されうる。

### 2.4 上層：現在文脈・意味・目的

現在の曲中の文脈、現在の目的、現在の意味解釈、現在の期待を扱う。

```text
M_B^context
=
現在の文脈において一時的に立ち上がっている関係構造
```

文化や経験の蓄積は`M_B^learned`に置き、上層はそれらが現在の曲や状況に応じて立ち上がった状態として扱う。したがって、上層は長期蓄積された構造と同じ固定層ではない。

---

## ■ 3. 層間の接続

基本の流れは下から上である。

```text
波形・物理関係
    ↓
基層の変換関係
    ↓
中核の蓄積構造との比較
    ↓
上層の文脈・意味・目的
```

ただし、処理は一方向に固定しない。

```text
上層の期待・目的
        ↓
注意の向け方、B、Γの選択
        ↓
同じ対象から異なるRIB_Bを取得し、異なる関係を回収
```

したがって、上層は物理波形を直接変更しなくても、何を有限作用断面として取り、何を聴き、何を保存し、何を破断とみなすかを変えうる。

基層も一方向の受動フィルタとは限らない。中核・上層の状態が注意、比較単位、時間統合、判定規則へ影響し、同じ物理状態差から異なる知覚差が立ち上がる可能性を残す。

この逆方向の作用は、物理側の検証結果と混同せず、知覚・解釈側の仮説として記録する。

---

## ■ 4. 物理側と知覚側の分離

次の三つを同一視しない。

```text
物理的関係が変化した
≠
その変化が現在の有限B/RIB_Bで検出された
≠
知覚上の破断として感じられた
```

さらに、知覚上の破断と音楽的意味の変化も同一ではない。

```text
知覚上の差が生じた
≠
learned音楽構造上の差として扱われた
≠
現在文脈上の意味が変化した
```

逆方向も同様である。

```text
物理的関係が変化していない
≠
知覚上の保存が成立する
```

知覚上のまとまり、音高、拍、和声、緊張感などを扱う場合は、物理側で使用したB・RIB_B・Γとは別のB・RIB_B・Γを定義する。

06〜09の結論を、そのまま聴覚上の知覚可能性へ拡張しない。

---

## ■ 5. 既知の信号処理とRDL側の成果

実験結果は、次の二層に分けて記録する。

### 5.1 信号処理として既知の部分

```text
周波数比
共通周期
有限観測
短時間現象のサンプル間見落とし
サンプリング格子の位相依存
```

これらをRDL音楽の新定理として主張しない。

### 5.2 RDLによる構造記述

RDL側で得られるのは、既知現象を次の構造へ配置することである。

```text
対象
× finite B
× actual RIB_B
× Γ
↓
保存／破断として記述された関係
```

この配置から、次の区別が得られる。

```text
値が変わる
≠
関係が変わる

関係が破れる
≠
破断が現在のRIB_Bで観測される

保存と判定される
≠
絶対的に保存している
```

さらに基層を扱う段階では、

```text
物理的差
≠
人間系で現れる差
≠
learned音楽構造による分類差
```

を分離して記録する。

この段階での成果は、音楽知覚の完成理論ではなく、物理・基層・learned・context間の写像と破断を追跡するための構造記述である。

---

## ■ 6. CoreとModuleの境界

Coreには、層をまたいで再利用できる最小文法だけを置く。

```text
B
SILN_music / W_music
RIB_B
M_B^device / M_B^analysis / M_B^listener / M_B^learned
F = interp(M_B, RIB_B)
E
finite review -> unresolved remainder H_vec
H = ||H_vec||
ξ
θ / M_Δ
preserve()
change()
stabilize()
destabilize()
shift()
blur()
break()
redistribute_relation_configuration()
```

物理側の具体的な検出規則、聴覚モデル、調律、和声、ジャンル、特定の知覚効果は、検証またはModuleとして保持する。

Coreへ昇格する条件は、少なくとも次の通りとする。

```text
複数の層または領域で再利用される
独立した検証条件を持つ
既存Coreの記述だけでは扱えない差を生む
特定文化圏や特定実装に依存しない
```

一つの実験で使われただけの変数や判定規則は、直ちにCoreへ追加しない。

`W_music` はMusic領域で対象側関係を記述・比較する補助役割、Γは各層の関係を抽出・比較・検証するAdapter・Module・検証系の規則として管理する。どちらもRDL基底量へ昇格させない。

したがって、Coreの状態記述`S_t`にΓを追加する変更ではない。

また、検証列で用いる`contract`、`processing request`、`activation bundle`などは、Music側のModule接続を追跡するための局所的な契約境界であり、現段階ではRDL Core Primitiveへ昇格させない。

基層についても同様に、完成した人体・神経モデルをCoreへ追加しない。物理と中核の間で反復して残る変換関係を、検証可能なModule候補として蓄積する。

---

## ■ 7. 検証の進め方

検証は、下位の条件を固定してから上位へ進める。

```text
1. 対象と既知モデルを固定する
2. Purposeと有限Bを明示する
3. 実際に取得・提示されたRIB_Bを明示する
4. Γを明示する
5. 保存・変化・破断の判定軸を分離する
6. F/F'を比較する場合は同じ更新前M_Bを固定する
7. Eを記録し、非ゼロEを直ちにHとしない
8. 局所吸収・説明・通常変化を検査し、有限review後のunresolved remainderだけをHへ入れる
9. 結果を対象側SILN/W候補、自己側M_B候補、E/H、ξに分けて記録する
10. 別のB、RIB_B、またはΓで結果がどう変わるか確認する
11. 物理差と人間側の応答差を分けて記録する
12. learned音楽構造による分類・解釈をさらに分離する
13. その後に文脈・意味へ接続する
```

一度に複数の条件を変えない。

特に、sample rate、sampling phase、波形モデル、判定規則を同時に変更しない。

基層検証では、さらに次を一度に混ぜない。

```text
物理刺激の変更
身体・知覚条件の変更
学習歴・文化条件の変更
現在文脈の変更
応答判定Γの変更
```

実験は、次の順で強度を上げる。

```text
既知関係の再現
→ 関係の時間変化
→ 短時間破断
→ Bの変更
→ RIB_Bの変更
→ Γの変更
→ 人間側応答との対応
→ learned構造との差分
→ 文脈・意味との接続
```

---

## ■ 8. 破断条件

次の場合は、上位概念を追加する前に設計を引き直す。

RDLでは事後的・逆算的な仮設形成そのものを禁止しない。RDLの基準は反証可能性ではなく、B・Γ・M_B候補のどこで保存が破れ、どの条件で構造を引き直すかを記録するSILN耐久検査システムにある。問題とするのは、観測後に形成した仮設を独立検査なしに確定構造へ昇格させること、または任意の結果に適合できるよう仮設を無制約に変更し、SILN耐久検査上の破断条件・再検査点を失うことである。

- 物理モデルと知覚モデルを同じ結果として扱っている
- Bを固定したまま、観測対象だけが変わったと解釈している
- 実際に取得したRIB_Bを明示せず、対象側SILN/Wそのものを入力断面とみなしている
- Γを隠したまま、保存・破断を対象の属性として記述している
- ΓをRDLの新しい基底量として扱っている
- W_musicをT0 primitiveとして扱っている
- M_Bを対象側の絶対属性として扱っている
- 対象側SILN/W候補と自己側M_B候補を混同している
- FとF'で異なる更新前M_Bを使っている
- 非ゼロEや|E|を有限reviewなしにHとしている
- Γ通過をもってSILNの絶対的な耐久証明とみなしている
- ξを既知の未観測内容そのもの、貯蔵庫、数値圧力、または直接操作対象として確定している
- Bを変えればξを最終消去できるものとして扱っている
- 既知の信号処理をRDL固有の新定理として主張している
- 基層を既知の聴覚・神経科学用語の一覧として先取りしている
- 基層とlearned中核を「生得／後天」の一軸だけで固定分離している
- `F_device`、`RIB_B^human`、`F_human` を同一の作用解釈として扱っている
- 上層の意味から逆算して得た対象側構造候補を、B・RIB_B・Γを明示した独立検査を経ずにM_Bとして確定している
- Coreが特定の音楽文化・知覚仮説・実装へ依存している

---

## ■ 9. 現時点の全体像

```text
対象側SILN_music / W_music
  ↓
各層のfinite B
（Purpose・対象範囲・interaction/action section・比較条件を含む）
  ↓
actual RIB_B
  ↓
Γ
（Adapter・Module・検証系の抽出・比較・破断判定規則）
  ↓
same pre-update M_Bで F / F' を形成
  ↓
E
  ↓ finite review
reviewed unresolved H / M_B候補 / ξ
  ↓
条件付き耐久検査記録

phys
  ↓
base
  ↓
learned
  ↓
context
```

基層を中心に見ると、

```text
物理状態差 ΔP
    ↓
B_base / RIB_B^base / Γ_base
    ↓
人間系による変換
    ↓
知覚・行動上の状態差 ΔR
    ↓
M_B^learned による分類・予測
    ↓
M_B^context による現在的意味
```

となる。

各層は独立した存在ではなく、下位層から上位層へ関係を渡しながら、上位層からBとΓの選択へ影響を返す。

ここでWは、現在のBとΓのもとで対象側関係を記述・比較するMusic-localな作業領域として読む。

ここでRIB_Bは、対象そのものではなく、その層のinteractionから有限Bで実際に切り出された作用断面である。

ここでΓは操作・検証側の規則であり、Coreの状態変数へ追加されたことを意味しない。

基層は固定された人体モデルではなく、物理側の状態変化と人間側の状態変化との間に反復して現れる変換関係を、B・RIB_B・Γのもとで抽出するネットワークとして扱う。

現在のRDL音楽は、物理側の観測・関係抽出層を固めながら、中核側のModule検証を進め、その両側から基層の安定写像・破断・残差を抽出できる位置へ進んでいる。

---

## ■ 10. 最短圧縮

```text
硬い物から始める。
ただし、硬さを絶対真理にしない。

対象側SILN/W
× finite B
× actual RIB_B
× Γ
    ↓
same frozen pre-update M_B
    ↓
F / F'
    ↓
E
    ↓ finite review
reviewed unresolved H

B
= Purpose
+ 対象範囲
+ interaction/action section
+ 選択関係
+ 比較条件

Γ
= 領域固有の
  抽出・比較・耐久検査規則

W_music / Γ
= Music-local auxiliary roles
≠ T0 primitive

Γ通過
≠ 絶対証明

phys
→ base
→ learned
→ context

物理
= 何が変化したか

base
= その変化が人間系に
  どのような差として現れたか

learned
= その差を既習音楽構造が
  何として扱ったか

context
= 今この文脈で
  それが何を意味するか

物理的変化
≠ 現在のRIB_Bで観測された変化
≠ 知覚上の破断
≠ 音楽的意味の変化

Coreは小さく保つ。
Moduleと検証記録で具体化する。
```

---

*v0.3：現行RDL_Core v2.3の有限境界モデルへ同期。旧EFPを `RIB -> finite B -> RIB_B` に置換し、対象側SILN/W・有限作用断面RIB_B・自己側M_Bを分離。BをPurpose・対象範囲・interaction/action section・選択関係・比較条件の有限固定として明示し、F/F'を同じ更新前M_Bで形成する条件、非ゼロEを直ちにHへ入れない有限review境界を追加。W_music / ΓはMusic-local補助役割として維持。*

*v0.2：基層を「物理状態差と人間側状態差を接続する変換関係ネットワーク」として明示。基層の抽出手順、learned中核との操作的区別、F_device→F_human間の中間写像を追加。ΓをSILN耐久検査に接続する領域固有検査規則として位置づけ、将来的な耐久検査記録・構造耐久証明書への接続可能性を明記。ξ残存性と文書ヘッダーをRDL文書規約に合わせて整理。*

*v0.1改訂：物理側の波形検証、基層・中核・上層の階層モデル、M_BとM_B候補の分離、Γの配置、CoreとModuleの境界を統合した全体設計方針として改訂。*
